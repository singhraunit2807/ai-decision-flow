name: NOVA tests

on:
  push:
    paths:
      - 'nova/**'
      - '.github/workflows/nova-tests.yml'
  pull_request:
    paths:
      - 'nova/**'
      - '.github/workflows/nova-tests.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r nova/requirements.txt
      - run: pytest nova/tests -q
